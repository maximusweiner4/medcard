{
  "name": "medcard",
  "private": true,
  "workspaces": [
    "server"
  ],
  "scripts": {
    "server": "npm run dev --workspace=server",
    "mobile": "npm run start --workspace=mobile"
  },
  "devDependencies": {
    "@types/react": "~19.1.10"
  }
}
